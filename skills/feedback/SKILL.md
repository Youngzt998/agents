---
name: feedback
description: Record a correction from the user into the routed feedback log (general lessons in ~/.agents/feedback/LOG.md with private identifiers genericized; overlay-repo lessons in the overlay's log) and propose a rule change if the pattern recurs. Use when the user says the output was wrong, unclear, badly worded, or invokes /feedback.
---

Follow ~/.agents/workflows/feedback.md exactly. The user's complaint is: $ARGUMENTS

If $ARGUMENTS is empty, ask in one sentence what went wrong.
